# =============================================================================
# .env.template
#
# Rename this file to .env (which is ignored by Git) and fill in the values
# below. Then run ./configure.sh to generate the final config files and install
# everything.
# =============================================================================

# Tinyproxy port
TINYPROXY_PORT=8888

# Comma-separated list of allowed IPs/networks. Example:
# 127.0.0.1,192.168.0.0/16,1.2.3.4/32
TINYPROXY_ALLOWED_IPS=127.0.0.1

# Basic Auth credentials for Tinyproxy
TINYPROXY_AUTH_LOGIN=yourlogin
TINYPROXY_AUTH_PASSWORD=yourpassword

# Certbot domain and email
CERTBOT_DOMAIN=example.com
CERTBOT_EMAIL=admin@example.com

# Paths to the SSL certificate and key (from Let's Encrypt)
SSL_CERT_PATH=/etc/letsencrypt/live/example.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/example.com/privkey.pem
